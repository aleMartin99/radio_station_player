import 'package:flutter/material.dart';

/// Class that contains colors of light theme.
class LightColors {
  /// Primary color.
  static const Color primaryColor = Color(0xFF524FA1);

  /// Color to be used on primary color.
  static const Color onPrimaryColor = Color(0xFFFFFFFF);

  /// Scaffold background color of the light theme.
  static const Color backgroundColor = Color(0xFFF1F2F6);

  /// Subtitle Text color of the light theme.
  static const Color subtitleTextColor = Colors.grey;

  /// Color for components located on backgroundColor
  static const Color onBackgroundColor = Color(0xFF667085);

  /// Color shadows
  static const Color shadowColor = Color(0xFFDADFF0);

  /// Error color.
  static const errorColor = Color(0xFFD50006);
}
